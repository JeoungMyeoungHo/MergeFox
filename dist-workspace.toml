[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.31.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell", "msi"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Release packaging is intentionally tag-only. Regular PR/push CI still runs
# build + test on Linux/macOS/Windows, while cargo-dist artifacts are built
# only when a version tag is pushed.
pr-run-mode = "skip"
# Extra static files to include in each App (path relative to this Cargo.toml's dir)
include = ["README.md", "LICENSE", "NOTICE", "CHANGELOG.md", "RELEASE.md"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
